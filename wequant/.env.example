# Copy this file to `.env` and fill your own values.
# IMPORTANT: `.env` contains sensitive login state. Do NOT commit it.
#
# Login state (choose ONE):
# ZSXQ_AUTH=...                       # Authorization header (if your browser sends it)
ZSXQ_COOKIE=zsxq_access_token=...     # Cookie login state from Network -> Request Headers -> cookie
#
# Required:
ZSXQ_GROUP_ID=15552882181122
#
# Optional behavior:
ZSXQ_OUT_DIR=D:\zxxq_xlsx
ZSXQ_SCOPE=with_files
ZSXQ_COUNT=20
ZSXQ_SLEEP_TOPICS=0.8
ZSXQ_SLEEP_DOWNLOAD_URL=2.5
ZSXQ_RESUME=1
# Default filename: keep the original name (recommended). Set to 1 to use "<file_id>_name.xlsx".
# ZSXQ_KEEP_ID_PREFIX=1
# When ZSXQ_KEEP_ID_PREFIX is NOT set, script will try to rename existing "<file_id>_name.xlsx" -> "name.xlsx".
# Set to 0 to disable that renaming behavior.
# ZSXQ_STRIP_EXISTING_ID_PREFIX=0
# Debug/acceptance only: stop after N successful downloads.
# ZSXQ_MAX_DOWNLOADS=3
#
# If your browser is using a local proxy (e.g. 127.0.0.1:7890), you may need these:
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890
