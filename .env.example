# ===== Host paths (Linux) =====
# Host-side repo root. For this workspace default is /data/quanteam
EAM_REPO_HOST=/data/quanteam

# Host-side artifacts directory (must be writable by EAM_UID/EAM_GID)
EAM_ARTIFACTS_HOST=/data/quanteam/artifacts

# Host-side data directory (must be writable by EAM_UID/EAM_GID)
EAM_DATA_HOST=/data/quanteam/data

# ===== Runtime =====
TZ=Asia/Taipei
EAM_ENV=dev

# Host IP for published ports (LAN access).
# - 0.0.0.0: expose to LAN (e.g. http://192.168.x.x:8002 and http://192.168.x.x:3002/ui)
# - 127.0.0.1: local-only
# - a specific LAN IP (e.g. 192.168.31.241): bind to that interface only
EAM_PUBLISH_IP=0.0.0.0

# API host port (host -> container:8000). Set this if 8002 is already in use on your machine.
EAM_API_PORT_HOST=8002

# UI host port (host -> nginx:80 -> api:8000). Set this if 3002 is already in use on your machine.
EAM_UI_PORT_HOST=3002

# ===== Permissions (Linux) =====
# Use `id -u` / `id -g` to fill your UID/GID to avoid root-owned files.
EAM_UID=1000
EAM_GID=1000

# .env should not be committed; commit .env.example only.
